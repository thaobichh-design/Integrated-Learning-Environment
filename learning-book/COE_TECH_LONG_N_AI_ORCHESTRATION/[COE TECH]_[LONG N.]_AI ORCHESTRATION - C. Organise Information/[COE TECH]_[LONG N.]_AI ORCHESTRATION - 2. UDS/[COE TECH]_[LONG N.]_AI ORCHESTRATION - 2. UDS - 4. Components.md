# Topic 2. UDS — Page 4: Components
*Phase C. Organise Information | Topic: 2. UDS | Page: 4. Components*
*Subject: AI Orchestration / Engineering | UDO: Build and master an OE System that any analyst/operator can use to build domain-specific UE systems. Effective = Sustainable, Efficient, Scalable.*
*Derives from: Topic 2 Page 3 (EPS) — each component implements/enables one or more principles (P3, P9, P11, P12, P15, P16).*

---

## Perspective Rule
> *All components are **Ultimately Effective System (UES)** tools and environment conditions that **enable the principles from Page 3 to operate reliably**. Each component is not generic — it specifically supports Deliberate Post-Build Reflection, Cross-Domain Forcing, Curriculum Discipline, and Principle Challenge in AI Orchestration context. Row subjects are components; questions are answered from the component's role in implementing each principle.*

---

## Column Key

| # | Group | Question |
|---|---|---|
| 1 | INTRODUCTION | What component is this? What does it enable? Why does it matter to the Learner? (Relevance) |
| 2 | INTRODUCTION | What is it precisely — and what is it NOT? (Precise Definition) |
| 3 | SUCCESS | How does the component operate when functioning as designed? (Success Actions) |
| 4 | SUCCESS | What ultimately causes the component to function as designed? (UDS) |
| 5 | SUCCESS | How does col 4 (UDS) cause the component to function as designed? (Success Mechanism) |
| 6 | SUCCESS | What principles is the UDS based on? (Success EPS) |
| 7 | SUCCESS | What tools do the ultimate drivers require? (Success Tools — UES) |
| 8 | SUCCESS | What environmental conditions do the ultimate drivers require? (Success Environment — UES) |
| 9 | FAILURE | How can the component fail to function as designed? (Failure Actions) |
| 10 | FAILURE | What ultimately causes the component to fail to function as designed? (UBS) |
| 11 | FAILURE | How does col 10 (UBS) cause the component to fail? (Failure Mechanism) |
| 12 | FAILURE | What principles are the failure causes based on? (Failure EPS) |
| 13 | FAILURE | What tools do the failure causes require? (Failure Tools — UES) |
| 14 | FAILURE | What environmental conditions do the failure causes require? (Failure Environment — UES) |
| 15 | LEARNER'S NOTE | If the component fails as designed, what should the Learner do? (What Else?) |
| 16 | LEARNER'S NOTE | Next Steps to Take (Now What? Now How?) |

---

## Layer Map (UES = 3 Causal Layers)

| Layer | Role | Components (this page) |
|-------|------|------------------------|
| **INFRASTRUCTURE (Layer 1)** | Runtime foundation — must exist first | Python 3; Anthropic API + Claude LLM |
| **WORKSPACE (Layer 2)** | Operating environment — requires Infrastructure | ILE Learning Book; Cursor IDE with Agent; ClickUp task tracking |
| **INTELLIGENCE (Layer 3)** | Cognitive amplification — requires Workspace | ILE Learning Agent (sparring partner); Agno reference architectures; Principle extraction template |

**Ordering rule:** INFRA components first, WORKSPACE second, INTEL third. All components must exist in this causal order for the principles to operate.

---

## Table

| Row | 1 · Relevance | 2 · Precise Definition | 3 · Success Actions | 4 · UDS | 5 · Success Mechanism | 6 · Success EPS | 7 · Success Tools (UES) | 8 · Success Environment (UES) | 9 · Failure Actions | 10 · UBS | 11 · Failure Mechanism | 12 · Failure EPS | 13 · Failure Tools (UES) | 14 · Failure Environment (UES) | 15 · What Else? | 16 · Next Steps |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **INFRA.1: Python 3 Runtime** | **Enables all framework execution for AI agents.** This is the foundational computational layer on which Agno, ILE Learning Book, and all Deliberate Post-Build Reflection tools run. Without Python, no agent decomposition practice is possible. | A working Python 3.10+ environment with package management (pip, uv, or poetry) installed on the Learner's machine or cloud compute. IS a local or cloud Python REPL that can execute `import agno`, `import ile`, and run agent code. IS NOT a GUI IDE alone (Cursor is IDE, not runtime). IS NOT optional for AI Orchestration — all agent work requires Python execution. | (1) Python 3.10+ is installed (`python --version`). (2) Package manager (pip or poetry) is functional. (3) Required packages can be installed: `pip install agno`, `pip install anthropic`, etc. (4) Agent scripts can be executed: `python agent.py`. (5) REPL is available for exploration (`python -i script.py`). | Learner's commitment to running all agent builds in Python locally (not just reading about agents or studying examples) — deliberate hands-on building practice is what drives the reflection that becomes principles. This commitment is the ultimate driver of using Python runtime at all. | Commitment to hands-on building → Python is used throughout learning → agents are built locally → each build forces decomposition decisions → reflection is then grounded in real execution experience → principles derived are traceable to actual builds, not hypothetical ones → skill compounds on lived experience. | P3(E): "Conscious extraction converts doing into knowing — extract after every build while context is fresh." Building requires Python runtime. P11(S): "Install structured forcing mechanism." The mechanism enforces: "principle written before next Python script opens." | Python package management (pip/poetry). Agno framework (imported and used in scripts). Terminal or REPL for running scripts. | Python environment variable PATH correctly configured. Terminal access without permission blockers. Version compatibility (Python 3.10+ for Agno). | Python is installed but not in PATH — `python` command is not recognized. Package manager is broken or outdated (`pip install` fails). Required packages cannot be installed (version conflicts, missing C libraries). Agent scripts fail to execute due to runtime errors. Learner defaults to studying/reading about agents instead of building them. | Environment setup friction — Python not in PATH, package install failures, version mismatches, or permission issues that make Python execution tedious. The friction causes the Learner to default to passive study (reading examples, watching videos) instead of active building (running scripts). No builds = no reflection = no principles. | Environment friction → Learner defers or skips building → builds less frequently → fewer decomposition decisions to reflect on → principle extraction is delayed or skipped → commitment to hands-on practice is broken → skill stays theoretical instead of experiential. | "Studying frameworks without building is enough." False — passive learning produces no reflection signals. "I can learn Agno by reading examples." Theoretical knowledge is not the same as hands-on decomposition skill. "Environment setup is not that important." Environment friction is a structural blocker to practice; it must be removed. | No Python or incompatible version. Package manager misconfigured. No terminal access. | Development environment locked down (corporate network, permission restrictions). No access to install packages. Long dependency resolution times that feel tedious. | If Python environment is broken: Set up a cloud-based Python runtime instead (Google Colab, Replit, AWS CodeWhisperer) that requires no local setup. The goal is getting Python execution working, not where it runs. Once runtime is functional, proceed to Agno setup. Do not let environment friction block practice. | INFRA.2: Anthropic API + Claude LLM. Then WORKSPACE.1–3. |
| **INFRA.2: Anthropic API + Claude LLM** | **Enables language model reasoning inside Agno agents.** This is the cognitive engine that powers all decomposition decisions and reflection sparring. Without Claude LLM, agents have no reasoning capability and Deliberate Post-Build Reflection cannot be sparring-partnered (no tool to ask "Is that the real reason?"). | A configured Anthropic API key (`ANTHROPIC_API_KEY` environment variable or config file) that allows Python code to call Claude models via the Anthropic SDK. IS integration: `import anthropic; client.messages.create()` in Agno agent code, or in ILE Learning Agent in Cursor. IS NOT a web UI (Claude.com is UI, not the API). IS NOT without cost — token usage is metered and charged. IS NOT a free or unlimited service — rate limits and quota apply. | (1) Anthropic API key is generated (console.anthropic.com). (2) Key is stored securely in environment variable (`export ANTHROPIC_API_KEY=sk-...`) or `.env` file. (3) Anthropic Python SDK is installed: `pip install anthropic`. (4) Integration test passes: `python -c "import anthropic; client = anthropic.Anthropic(); print(client.messages.create(...))"`. (5) Agno agents can call Claude in `@tool` decorated functions. | Access to Claude LLM via Anthropic API — the availability of a high-quality language model that can be invoked inside agent code and inside ILE Learning Agent (Cursor) to ask sparring questions. This access is the enabler of all reflection. | API access → Claude available inside agent code → agent can call Claude for reasoning/decomposition → Claude available to ILE Learning Agent in Cursor → Agent can challenge principles ("Is that the real reason?") during reflection → Learner's principles are sharpened through sparring → compounding accelerates. | P16(E): "Challenge extracted principle in same session: 'Is that the real reason? What would falsify it?' in the same session, while context is fresh." Claude LLM is the sparring partner that asks this question. P12(Sc): "Write explicit multi-domain curriculum commitment...and ILE Learning Agent enforces it." The Agent is Claude-powered. | Anthropic Python SDK. API key (generated, not shared, not in public repos). Environment variable management tool (`.env` file, or shell profile). | Secure API key storage (not hard-coded in scripts or committed to Git). Network access to Anthropic API endpoints. Rate limit awareness (metering to avoid unexpected charges). Token budget tracking. | API key is missing, invalid, or expired — calls to Anthropic fail with 401 Unauthorized. API key is hard-coded in a script and committed to Git — security risk, key must be revoked. Rate limits exceeded — Learner hits quota and subsequent calls fail until quota resets. No Claude reasoning available → Learner cannot call Claude inside agent code → agents have no reasoning capability → no sparring partner for reflection → principles are not challenged and refined. | API key missing/expired or hard-coded insecurely. Rate limits exceeded due to excessive use or unoptimized calls. Network issues blocking API access (firewall, proxy, connectivity). Learner defaults to trying to reflect alone without Claude sparring. | Missing key → agents fail silently or with cryptic API errors. Expired key → calls fail; Learner does not realise the cause immediately. Hard-coded key → security incident waiting to happen. Exceeded limits → Learner frustrated by failures, tries to work around by not using Claude. Reflection without sparring → principles stay vague because no one challenges them; compounding stalls. | "I can just use ChatGPT web UI instead of the API." Different product, different integration; Cursor integration requires Anthropic API. "API key is fine to commit to a public repo as long as I'll delete it later." It is already compromised if committed; the key must be revoked immediately. "I don't need sparring; I can challenge my own principles." Self-critique is weaker than external sparring; alone, principles stay under-examined. | No Anthropic API key. Anthropic SDK not installed. Network blockers (corporate firewall blocking API endpoints). | No API budget (account not set up, payment method missing). API quotas are low or no credits. Team/org policies forbidding external API use. | If API is not accessible: (1) Check `echo $ANTHROPIC_API_KEY` to confirm key is set. (2) Test connectivity: `curl https://api.anthropic.com/health` (or similar). (3) If key is missing, generate one at console.anthropic.com. (4) If connectivity is blocked, request network admin to allow Anthropic API endpoints. (5) Fallback (last resort): Use a free LLM API (e.g. Ollama local, open-source models) as sparring partner (less capable, but functional). Do not skip sparring entirely. | WORKSPACE.1: ILE Learning Book (structured place to write principles that Claude challenges). |
| **WORKSPACE.1: ILE Learning Book (Structured Repository)** | **Enables persistent principle storage and retrieval across builds.** This is the central artifact where extracted principles live and are versioned. Without a structured place to write principles, extraction is deferral-prone (written in scattered notes, emails, or not at all). ILE Learning Book is the gate-keeper: principles must be written here before next cycle opens. | A Git repository (`learning-book/` folder) structured per ILE specification (A. Subject Roadmap, B. Capture, C. Organise, D. Distill, E. Express, plus metadata files). One file per page (`2. UDS - 3. Principles.md` is T2.P3). Session Log and Decision Log in A (Subject Roadmap A file). IS a local Markdown + Git structure. IS NOT a wiki, blog, or external tool (Notion, OneNote, etc.). IS NOT optional for curriculum discipline — writing principles in ILE is what binds the commitment. IS NOT "I'll keep principles in my head or in scattered notes." | (1) Learning Book folder structure is initialized (per ILE README). (2) Session Log in A is updated at every session start and approval. (3) Principle is written in the target page file (e.g. `T2.P3.md` for Topic 2 Page 3 principles). (4) Principle is committed to Git: `git add T2/page-3.md; git commit -m "T2.P3: principle X approved"`. (5) Next cycle does not open until the commit is complete. The gate is: principle in ILE = cycle unblocked. | Structured storage with Version control (Git) that creates a persistent, time-stamped record of every extracted principle — making the principle retrievable, comparable, and holdable accountable in future builds. | Version control → every principle has a commit timestamp and author → principle is retrievable and history-traceable → next build can read previous principles and apply them → principles accumulate with continuity → compounding happens with reference. | P3(E): "Extract principle in explicit language after every build, in same session, before next cycle opens." ILE Learning Book is the exclusive write target; no principle counts unless written in ILE. P15(S): "Principle written in fixed format (Decision; Outcome; Rule) with gate blocking next cycle until complete." Git commit is the gate signal. | Git (local or cloud-hosted). Markdown editor (any text editor). Terminal for commit commands. Optional: Cursor IDE with Git extension (tightly integrated). | Reliable file storage (local SSD, cloud sync like Google Drive or GitHub). Network access if using remote Git (GitHub, GitLab). No permission blockers preventing Git commits. Clear folder structure so Learner always knows where to write next principle. | Learning Book structure is not set up — Learner does not know where to write principles. Principles are scattered in notebooks, emails, Slack, or private notes instead of ILE. ILE structure is set up but principles are written in scattered locations (not committed to Git). Git commits are infrequent or skipped — principle is written but not persisted (accidentally deleted, lost on machine reset). Learner defaults to deferral: "I'll write the principle later when I have time to organize it." | No structured Learning Book. Principles scattered across multiple tools (notes app, email, Slack). No version control — principles are lost or overwritten. No discipline binding: "principle in my notes" does not feel as binding as "committed to Git." Deferral is easy when structure is absent. | Scattered principles → next build cannot easily find and apply previous principles → principles are not referenced or are re-invented → effort is wasted → compounding does not happen. Deferral culture → principles are perpetually deferred → next cycle opens without previous principle → reflection never happens consistently → skill stays linear. | "I'll keep principles in a notebook." Notebooks get lost; version control does not exist; retrieval is manual. "Committing to Git is overhead." Overhead of 30 seconds per cycle is worth the certainty of not losing principles. "My memory is good enough." Memory fades; written and committed principles persist. | No Learning Book structure. Git not initialized. Scattered principle-writing locations. | No access to initialize a repo (permission issues). Storage quota limits (limited cloud space). | If Learning Book structure is missing: (1) Create folder `learning-book/{SUBJECT}/C. Organise Information/{TOPIC}/`. (2) Create A (Subject Roadmap) file at root. (3) Initialize Git: `git init; git add .; git commit -m "Init ILE Learning Book"`. (4) For next principle: write in target page, then commit. The setup is a one-time cost; persistence is permanent. | WORKSPACE.2: Cursor IDE with ILE Learning Agent integration. |
| **WORKSPACE.2: Cursor IDE with ILE Learning Agent** | **Enables in-IDE sparring partner for principle challenge and curriculum enforcement.** Cursor with ILE Learning Agent integrated (via CLAUDE.md + instructions) is where the Learner writes agent code and where the Agent provides real-time feedback on principles, curriculum gates, and reflection quality. Without this integration, sparring and enforcement are out-of-band (separate tools, slow feedback). | The Cursor IDE configured with ILE Learning Agent active (CLAUDE.md auto-loaded at workspace start, Subject Roadmap A file in scope). Agent reads current session state (CLAUDE.md + A) and provides in-chat feedback on: principle extraction, curriculum completion, gate enforcement ("You haven't completed cross-domain practice yet — cannot proceed to deep Investment agents work"). IS tight integration: Agent is in the same window as code and terminal. IS NOT a separate web chat (ChatGPT web would be out-of-band, slower feedback). IS NOT optional for curriculum discipline — the Agent's refusal to proceed enforces commitment. | (1) CLAUDE.md is auto-loaded (workspace-level setting). (2) ILE Learning Agent is configured as the default agent for Cursor. (3) Subject Roadmap A file is in workspace (Cursor can read it). (4) Learner writes code in Cursor editor; when asking for next cycle, Agent reads CLAUDE.md and A and responds: "Curriculum commitment shows cross-domain practice incomplete. Let's finish Domain B before returning to Investment agents." (5) Learner either complies (proceeds to Domain B work) or explicitly overrides (visible defection from commitment). The gate is: Agent's feedback = reality check on curriculum compliance. | In-IDE integration of Agent + access to CLAUDE.md and Subject Roadmap A — so Agent can read the Learner's binding curriculum commitment and enforce it in real-time without Learner having to ask permission or context-switch to another tool. | Integration → Agent reads A at every session start → curriculum gates are visible to Agent → when Learner asks for next cycle, Agent checks gates and enforces them in real-time → User cannot proceed without explicit override → discipline is external to moment-to-moment willpower → enforcement is consistent. | P11(S): "Install structured forcing mechanism before solo learning begins." Cursor+Agent is the mechanism. P12(Sc): "Write curriculum commitment in Subject Roadmap A...ILE Learning Agent enforces it." Agent reads A and enforces. P16(E): "Challenge principle in same session; sparring partner asks forcing question." Agent is the sparring partner (powered by Claude LLM). | Cursor IDE. CLAUDE.md file (auto-loaded). Subject Roadmap A file (readable by Agent). ILE Learning Agent (active workspace agent). | Cursor is the primary IDE for this Subject (not VS Code, not other IDEs running independently). CLAUDE.md is in workspace root where it auto-loads. A is committed to Git and always up-to-date. Agent has read access to A and CLAUDE.md. Session starts with Agent reading these files. | Cursor is installed but CLAUDE.md is not in workspace or is not set to auto-load — Agent does not read CLAUDE.md or A. User switches to a different IDE (VS Code, PyCharm) and loses Agent integration. A file is not updated — Agent reads stale curriculum state and cannot enforce correctly. User ignores Agent feedback ("The Agent says cross-domain practice is incomplete, but I'll start Investment agents anyway") — enforcement fails when User overrides without visible friction. | CLAUDE.md not auto-loaded. A file not in scope or not readable by Agent. User workflow in separate IDE. A file is stale (not updated after approvals). User treats Agent suggestions as advisory, not binding. | Enforcement breaks when CLAUDE.md is not loaded or A is not updated → Agent has no access to curriculum commitment → gates cannot be enforced → Learner can skip cross-domain practice → discipline collapses. Stale A → Agent enforces outdated gates → Learner becomes frustrated with Agent accuracy → stops trusting Agent's enforcement. | "The Agent's suggestions are just advice — I can ignore them." False — the Agent's access to your written commitment in A makes the suggestion binding; ignoring it is visible defection from your own contract. "I'll switch to VS Code for some tasks." Different IDEs break Agent integration; integration must be continuous. "I'll update A later." Stale A makes enforcement inaccurate; A must be updated immediately after every approval. | CLAUDE.md not in workspace. A not readable by Agent. User primarily developing in different IDE. A is out of date. | No permission to set workspace-level agent settings. Multiple projects/branches where agent behavior needs to differ. | If Cursor integration is weak: (1) Open Cursor workspace settings. (2) Ensure CLAUDE.md is in workspace root and set to auto-load (`"claude_code.startup_commands"` includes read of CLAUDE.md). (3) Ensure A (Subject Roadmap) is always readable by Agent (committed to Git, not gitignored). (4) Make Cursor your primary IDE for this Subject (do not switch to VS Code for some tasks). (5) After every approval, update A immediately. The integration is the enforcement mechanism; do not weaken it. | WORKSPACE.3: ClickUp task tracking for curriculum milestones. Then INTEL.1–3. |
| **WORKSPACE.3: ClickUp Task Tracking (Curriculum Milestones)** | **Enables visible curriculum progress and optional hard gate enforcement.** ClickUp tasks with dependencies allow Learner (or team) to see cross-domain practice progress and optionally block Investment agent tasks until prerequisites are complete. Without a task system, curriculum commitment is verbal/file-based only; with ClickUp, it is operationally enforced (tasks cannot be moved forward until blockers clear). | A ClickUp workspace or project configured with curriculum milestones as tasks. Example structure: "Domain A Decomposition (blocking task)" → "Domain B Decomposition" → "Cross-Domain Comparison" → "Domain B COMPLETE (gate)" → "Investment Research Agent Build" (blocked until gate is cleared). Task dependencies prevent opening Investment Research tasks until cross-domain gate is marked complete. IS hard gate: moving a task forward is functionally blocked, not just advisory. IS NOT mandatory if Learner trusts their own A commitment + Agent enforcement; optional escalation. IS NOT replacing A or Agent — it is complementary operationalization. | (1) ClickUp workspace is created with Learner's project. (2) Curriculum milestones are defined as tasks with clear naming ("Domain A Decomposition", "Cross-Domain Comparison", "L2 Learning Gate") and sequencing. (3) Task dependencies are set: Domain A blocks Domain B, Domain B blocks Comparison, Comparison blocks Investment Research Build. (4) At every session, Learner checks: "Is the blocking task complete?" If not, work on the blocking task before opening the next task. (5) When a blocking task is marked complete, downstream tasks become available. | Operational enforcement via task system — tasks cannot be marked forward until their blockers are complete, creating a structural forcing function independent of willpower or Agent feedback. | Blocking task structure → User cannot open Investment task without closing prior tasks → User is forced into correct sequence → cross-domain practice happens on schedule even when urgency pulls toward Investment agents → discipline is operationally enforced, not just contractually binding. | P11(S): "Install structured forcing mechanism." ClickUp task blocking is one such mechanism (Agent/A is another). P12(Sc): "Write curriculum commitment in A...bind yourself to sequence...ClickUp milestones track it visibly." | ClickUp account and workspace. Task management (create, update, block). Integrations (optional: Slack notifications for task updates, GitHub for commit linking). | Learner is the task owner (can create, update, block tasks). Team visibility (optional — tasks can be private or shared). Notifications enabled so Learner is aware when a blocking task is cleared. | ClickUp is set up but curriculum milestones are not defined as tasks — no blocking structure exists. Task dependencies are not used — all tasks are open simultaneously and Learner can jump to any task. Blocking tasks are set up but Learner overrides them ("I'll start Investment anyway and do cross-domain later") — operational block is ignored. User workflow is not ClickUp-aware — Learner works in code/notes, forgets to update ClickUp tasks. | No curriculum milestones in ClickUp. No task blocking. No ClickUp check-in routine. Learner does not review ClickUp before starting work. ClickUp becomes stale (tasks not updated). | Stale task structure → Learner ignores ClickUp because it is not accurate → discipline devolves to agent enforcement or nothing. Task blocking ignored → Learner can jump around sequence → cross-domain practice is skipped → commitment is broken. | "ClickUp is overhead — A + Agent enforcement is enough." True if Learner fully trusts A + Agent; false if Learner needs visible, operational structure. "I'll just remember the sequence." Memory and urgency do not align; structure is more reliable. "ClickUp is for teams — I'm solo." ClickUp is also useful for solo structure; team-optional. | No ClickUp workspace. Task dependencies not configured. | No access to create ClickUp projects. Team workspace where individual task blocking is not allowed. | If ClickUp is absent or unused: Set it up (free tier available). Create curriculum milestone tasks with blocking dependencies. Before every learning session, check: "Is my blocking task complete?" If not, work on it. When complete, mark task done (next task unlocks automatically). This adds 30 seconds to session start but provides operational certainty. | INTEL.1: ILE Learning Agent (the sparring partner). |
| **INTEL.1: ILE Learning Agent (Sparring Partner & Enforcer)** | **Enables real-time principle challenge and curriculum enforcement in chat.** The ILE Learning Agent (Claude LLM powered, integrated in Cursor per CLAUDE.md) is the sparring partner that asks "Is that the real reason?" after principle extraction, and the enforcer that checks curriculum gates and refuses to proceed until gates are cleared. Without an external agent asking hard questions, principles stay under-examined. | Claude LLM configured to act as ILE Learning Agent (via system prompt in CLAUDE.md). Agent reads CLAUDE.md + Subject Roadmap A at session start. Agent has access to: current subject, learning-book folder structure, ILE templates, approved pages, and curriculum commitment in A. Agent's role: (1) Challenge principles (P16); (2) Enforce curriculum gates (P12); (3) Guide session flow per EOP. IS in-session (chat, real-time feedback). IS NOT a general-purpose chatbot (ChatGPT web UI lacks A access and CLAUDE.md specificity). | (1) At session start, Agent reads CLAUDE.md and A to understand curriculum state. (2) Learner extracts a principle: "After this build, the blocker is that I was iterating on one variable at a time instead of testing boundaries together. Rule: test all boundaries in one build." (3) Agent asks: "Is that the real reason the blocker occurred? What would falsify it? What if the build succeeded anyway — would you still attribute it to boundary testing?" (4) Learner revises: "When three or more boundary variables are in play, testing them in isolation is the blocker. Rule: test all boundary variables together in one run; if they pass, the boundary decision is valid." (5) Revised principle is sharper and more testable. (6) Agent confirms curriculum gate before next cycle: "Subject Roadmap A shows cross-domain practice still needed. Before we start Investment agents, finish Domain B. Ready?" | Claude LLM reasoning capability + access to CLAUDE.md + access to Subject Roadmap A — the Agent can read your commitment, check your curriculum gates, and challenge your principles with specificity because it knows your context. | Access to context → Agent knows curriculum commitment → Agent knows which cross-domain domains are required → Agent asks principled challenge questions because it knows the principle topic → challenges are specific, not generic. | P16(E): "Challenge extracted principle in same session...'Is that the real reason? What would falsify it?'" Agent is the voice asking these questions. P11(S): "Install forcing mechanism...Agent's refusal to proceed with deep work until gates are cleared is the mechanism." Agent enforces gates. | Claude LLM (Claude 3.5 Sonnet or better for reasoning). CLAUDE.md file (system context). Subject Roadmap A file (state). Access to learning-book folder and approved pages (context retrieval). Terminal in Cursor (for executing commands while Agent provides guidance). | Learner treats Agent feedback as binding (not just suggestions). Agent is the primary in-IDE assistant (not a secondary tool). Curriculum commitment in A is visible to Agent. Regular session starts with Agent reading A. | Agent is present but CLAUDE.md is not loaded (Agent has no ILE context). Agent is available but Learner treats suggestions as optional ("Agent says cross-domain is needed, but I'll start Investment anyway"). A is not updated so Agent reads stale curriculum state. Learner does not ask for principle challenge after extraction — defers reflection. Agent role is not understood (Learner thinks Agent is just a coding assistant, not a learning enforcer). | CLAUDE.md not auto-loaded. A not updated. Learner ignores gate enforcement. No principle challenge is requested. Agent is treated as generic chatbot, not ILE-specific enforcer. | Agent cannot access curriculum state → enforcement fails. Stale A → Agent enforces wrong gates. Learner ignores enforcement → discipline collapses. No principle challenge → principles stay vague → compounding stalls. | "The Agent is just a coding assistant." Agent's real role is enforcing your learning discipline and challenging your principles; acknowledge this. "I can decide when to apply the Agent's gates." The gate is in A, your written commitment; Agent is just reminding you of your own contract. "I don't need an external agent to challenge my principles." Self-critique is weaker; external challenge forces precision. | CLAUDE.md not in workspace. A not readable by Agent. Agent not configured as primary in-IDE assistant. | Team/org policies on LLM API use. Cursor workspace constraints. | If Agent is weak or unused: Reconfigure Cursor so Claude is the workspace agent (not another AI). Ensure CLAUDE.md is auto-loaded and A is in scope. After extracting a principle, explicitly ask the Agent: "Challenge my principle: is that the real reason? What would falsify it?" Wait for feedback before moving on. Over 3–5 cycles, challenge becomes routine and principles naturally sharpen. | INTEL.2: Agno reference architectures from multiple domains. |
| **INTEL.2: Agno Reference Architectures (Multi-Domain Examples)** | **Enables cross-domain pattern recognition and principle testing.** Agno examples from multiple unrelated domains (e.g. Finance, Healthcare, Logistics, Customer Support) provide concrete evidence of how decomposition boundaries repeat despite surface differences. Studying references while extracting principles enables the Learner to ask: "Did boundary decision X work the same way in Finance as it did in Healthcare?" | A curated collection of Agno agent/team/workflow examples from different domains, all demonstrating the same universal decomposition structure (specialist agents → structured flows → clean handoffs). References are NOT all from one domain (Finance only); they span 2+ domains. Examples could be: Agno's official GitHub examples, custom examples the Learner builds for this course, or published Agno case studies. IS study material for cross-domain comparison (P9). IS NOT meant to be copied verbatim; instead, studied to understand where boundaries were placed and why. | (1) Learner studies Agno Finance example (research analyst agent, risk analyst agent, coordinator handing off context). (2) Learner decomposes their own Finance build (same pattern: research → risk → handoff). (3) Learner extracts principle: "Research and Risk decisions can be decoupled because outputs are narrative (research thesis) and assessment (risk score); flows are one-way (research to risk)." (4) Learner then studies Agno Healthcare example (intake nurse agent, diagnosis agent, treatment agent). (5) Learner notices the same pattern: specialist roles with narrative handoffs. (6) Learner writes: "Cross-domain pattern seen: whenever domain has narrative handoffs from one specialist to another, decompose by role. Rule applies to Finance (research → risk) and Healthcare (intake → diagnosis → treatment)." (7) Universal pattern is explicit from cross-domain evidence. | Availability of reference examples from multiple domains — the Learner can see the same decomposition structure (roles, flows, handoffs) repeated across unrelated domains, which makes the universal pattern visible and testable. | Multiple domains → same structure visible in all → pattern is universal, not domain-specific → principle extraction is validated across domains → next build, Learner applies the universal principle instead of domain-specific thinking → decomposition is faster and more reliable → compounding accelerates. | P9(E): "Decompose 2+ completely different domains back-to-back with explicit comparison before deep single-domain mastery to prevent pattern blindness." References make cross-domain comparison concrete and grounded in published examples. P16(E): "Challenge principle...test whether principle holds across contexts." References provide test cases. | Agno GitHub repository (examples folder). Published Agno case studies or blog posts. Custom-built examples (Learner builds mini-agents in unfamiliar domains just to study structure). Optional: Agno documentation with architecture diagrams. | Access to Agno examples (GitHub public). Time to study examples (reading, running, modifying them). A domain problem or example with enough complexity to force genuine decomposition decisions (trivial examples hide patterns). | No Agno examples are studied — Learner only builds in their primary domain (Investment). Examples are studied from one domain only (Finance only; no Healthcare, Logistics, etc.). References are copied verbatim without understanding why boundaries were placed (cargo-cult pattern reuse). Learner does not compare references ("This Finance example has 3 agents, my Healthcare build has 4 — they must be completely different"). | No reference study. Single-domain immersion. Cargo-cult copying without pattern understanding. No cross-domain comparison in studies. | No cross-domain contrast → pattern blindness persists → next domain still feels unique → compounding fails. Cargo-cult copying → boundaries are arbitrarily copied without understanding → principles are "Do what Agno did," not "Understand why Agno's boundary decision worked." | "Studying examples is slower than just building my own domain." Building alone teaches domain-specific decomposition; studying examples teaches universal decomposition. Both are needed. "I'll study examples after I master my primary domain." Later rarely comes; cross-domain study must happen first (before domain expertise locks in domain-specific thinking). | No Agno examples available (private repo, not indexed). | Access restrictions (corporate network, no GitHub access). Time constraints. | If references are not available: (1) Use Agno's public documentation and architecture diagrams (patterns are visible even without full code). (2) Build mini-agents yourself in 2 unfamiliar domains using Agno templates (Finance + Healthcare; or Logistics + Support). (3) Compare your two builds side-by-side: roles, flows, handoffs. (4) Write: "Same pattern in both: [role1] → [role2] → [handoff type]." This custom cross-domain comparison is as valuable as published references. The goal is seeing the pattern repeat across domains, not studying specific examples. | INTEL.3: Principle extraction template (final component). |
| **INTEL.3: Principle Extraction Template (Fixed Format + Checklist)** | **Enables consistent, precise, same-session principle extraction.** The fixed-format template (Decision; Outcome; Rule; Challenge checklist) provides a structure that forces precision and blocks lazy/vague extraction. Without format, principles are freeform notes that vary in quality and are easy to defer. Format + checklist make extraction reliable. | A Markdown template or checklist in Session Log or page-specific log: (1) `Decision: [What boundary was tested in this build? What variable(s) changed?]` (2) `Outcome: [What happened as a result? What was the pass/fail result?]` (3) `Rule: [If [decision condition], [outcome result occurs]. Apply rule: [repeatable action for next build].]` (4) `Challenge checklist: ☐ Challenged? ☐ Revised for precision? ☐ Traceable? ☐ Testable?` IS a structured template, not freeform notes. IS NOT optional or flexible — same format every time. IS NOT a journal entry or blog post (those are useful but not the same as the extraction template). | (1) Build completes. (2) Open ILE Learning Book, navigate to Session Log. (3) Fill in template immediately (same session): "Decision: Tested coupling vs decoupling of Research and Risk agents. Changed: whether Risk agent depends on Research agent's state. Outcome: Decoupled version reduced integration latency by 50%; coupled version had state synchronization overhead. Rule: When agent outputs are narrative (not state-dependent), decouple them. Condition: narrative handoff. Action: separate agents into parallel, not sequential." (4) Check challenge checklist: "Is that the real reason for the latency? What would falsify it? [Yes, if decoupling without narrative outputs, latency would not improve. Rule is conditional on narrative outputs.] Revised for precision? [Yes, added narrative-output condition.]" (5) Mark checklist complete. (6) Commit to Git. Gate is now cleared; next cycle can open. | Structured, consistent format for extraction — every principle follows the same shape (Decision; Outcome; Rule) so the Learner's mind learns to think in this shape, and principles are naturally precise because the format forces precision. | Format structure → principles follow a pattern → pattern becomes automatic → Learner starts thinking in (Decision; Outcome; Rule) even before writing → principles are naturally precise before they're written → challenge is built into the thinking process. | P3(E): "Extract principle in explicit language after every build, in same session, before next cycle opens." Template is the vehicle for extraction. P15(S): "Principle written in fixed format (Decision; Outcome; Rule) with gate blocking next cycle until complete." Template is the format; checklist is the gate. P16(E): "Challenge principle in same session." Checklist includes challenge step. | Markdown template (can be text file, Google Doc, or notion). Checklist (text checkboxes, or Notion database, or simple pen-and-paper). Session Log in A (where template is filled in). | Same-session access to Session Log for writing (not deferred). Learner is willing to use a rigid template (not flexible, not pretty, but precise). A refresher on the template (printed or bookmarked) so Learner remembers the format. | Template is not used — principles are written in random formats (journal, email, Slack, scattered notes). Template is used but not strictly — Decision + Outcome written, but Rule is vague ("decomposition is important"). Challenge checklist is present but skipped ("I'll challenge later"). Principles are written days later (deferred extraction, not same-session). Principles are written but not committed to Git. | No template. Principles freeform. Template not enforced (Learner uses when they feel like it). Challenge checklist not used or skipped. Extraction deferred or scattered. No Git commit. | Freeform principles → variable quality, hard to compare, easy to defer. No checklist → principles are not challenged → stay vague → compounding stalls. Deferred extraction → context lost → principle is imprecise from memory. | "A simple template is too rigid." Rigidity forces precision; without it, vagueness is easier than precision. "I can write better principles without a template." Few people can; most people need structure to be precise. "I'll challenge principles later when I have examples." Later never comes; challenge same-session while context is fresh. | No template available. Learner is resistant to structure. | | If extraction is scattered or vague: Install the template now. (1) Copy the template into your Session Log: "Decision: __; Outcome: __; Rule: __; Challenge: ☐". (2) After next build, fill it in while context is fresh (do not defer). (3) Ask yourself: "Is that the real reason? What would falsify it?" Answer in the template. (4) Revise Rule until it passes challenge. (5) Commit to Git. (6) Repeat every cycle. Over 3–5 cycles, using the template becomes automatic and principles naturally sharpen. | All 3 INTEL components enabled → full Intelligence layer operational → Learner can extract, challenge, and refine principles reliably → P3, P15, P16 are operationalised → same-session extraction is consistent → principles compound. |

---

*7 rows · 7 components (INFRA 2 + WORKSPACE 3 + INTEL 2) · All components trace to principles from T2.P3.*
*INFRA.1–2 are prerequisites. WORKSPACE requires INFRA. INTEL requires WORKSPACE. Each component listed in order — do not skip.*
*Next page (T2.P5 EOP): How to operationalise all 6 principles + 7 components in a repeatable procedure.*
