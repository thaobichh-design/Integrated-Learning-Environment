# Topic 2. UDS — Page 5: Steps to Apply
*Phase C. Organise Information | Topic: 2. UDS | Page: 5. Steps to Apply*
*Subject: AI Orchestration / Engineering | UDO: Build and master an OE System that any analyst/operator can use to build domain-specific UE systems. Effective = Sustainable, Efficient, Scalable.*
*Derives from: Topic 2 Pages 1–4 — this EOP operationalises all 6 principles (P3, P9, P11, P12, P15, P16) and 8 components from T2.P4.*

---

## Perspective Rule
> *All steps in this EOP are answered from the **step's own perspective as an action unit**. Col 1 states which UBS element the step disables or which UDS element it enables. Col 4 states what drives this step to succeed. Col 10 states what ultimately blocks this step. Steps are sequential — each gate must clear before the next step opens.*

---

## What This EOP Operationalises

| Step | Primary Purpose | Principle Applied | UBS Overcome / UDS Enabled |
|---|---|---|---|
| STEP.1 | Domain selection via curriculum gate | P12(Sc); P11(S) | Disables UBS.UD (Multi-Domain Curriculum Discipline blocked without enforcement) |
| STEP.2 | Execute a bounded agent build | P3(E); P11(S) | Enables UDS.UD (Deliberate reflection requires a build to reflect on) |
| STEP.3 | Open extraction template immediately post-build | P3(E); P15(S) | Disables UDS.UD.UB (Reflection bypass — open template blocks the bypass) |
| STEP.4 | Fill extraction template (Decision → Outcome → Rule) | P3(E); P15(S) | Enables UDS.UD (Principle extraction converts build into knowledge) |
| STEP.5 | Challenge the principle with ILE Learning Agent | P16(E) | Enables UDS.UD.UD (Extraction gate — challenge sharpens rule before it is locked in) |
| STEP.6 | Commit refined principle to ILE Learning Book (Git) | P3(E); P15(S) | Disables UBS.UD.UB (Reflection bypass — Git commit is the structural gate) |
| STEP.7 | Cross-domain comparison + curriculum update | P9(E); P12(Sc) | Disables UBS (Pattern Blindness — explicit cross-domain comparison makes universal pattern visible) |

---

## Column Key

| # | Group | Question |
|---|---|---|
| 1 | INTRODUCTION | Why does this step exist? Which UBS does it overcome or which UDS element does it enable? (Relevance) |
| 2 | INTRODUCTION | What exactly is the step — action + verifiable output? (Precise Definition) |
| 3 | SUCCESS | How is this step executed when functioning as designed? (Success Actions) |
| 4 | SUCCESS | What ultimately drives this step to succeed? (UDS) |
| 5 | SUCCESS | How does col 4 (UDS) cause this step to succeed? (Success Mechanism) |
| 6 | SUCCESS | What principles is the step's success based on? (Success EPS) |
| 7 | SUCCESS | What tools enable this step? (Success Tools — UES) |
| 8 | SUCCESS | What environmental conditions does this step require? (Success Environment — UES) |
| 9 | FAILURE | How can this step fail to execute as designed? (Failure Actions) |
| 10 | FAILURE | What ultimately blocks this step? (UBS) |
| 11 | FAILURE | How does col 10 (UBS) cause this step to fail? (Failure Mechanism) |
| 12 | FAILURE | What principles does the failure exploit? (Failure EPS) |
| 13 | FAILURE | What tools are involved in the failure? (Failure Tools — UES) |
| 14 | FAILURE | What environmental conditions enable the failure? (Failure Environment — UES) |
| 15 | LEARNER'S NOTE | If this step fails, what should the Learner do? (What Else?) |
| 16 | LEARNER'S NOTE | Next step (Now What? Now How?) |

---

## Table

| Row | 1 · Relevance | 2 · Precise Definition | 3 · Success Actions | 4 · UDS | 5 · Success Mechanism | 6 · Success EPS | 7 · Success Tools (UES) | 8 · Success Environment (UES) | 9 · Failure Actions | 10 · UBS | 11 · Failure Mechanism | 12 · Failure EPS | 13 · Failure Tools (UES) | 14 · Failure Environment (UES) | 15 · What Else? | 16 · Next Steps |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **STEP.1: Domain Selection via Curriculum Gate** | Disables UBS.UD: Multi-Domain Curriculum Discipline (blocked without enforcement). Without selecting the correct domain in the correct sequence, the Learner defaults to their comfort domain (Investment) — deepening pattern blindness instead of enabling cross-domain generalization. This step enforces the curriculum commitment before any build begins. | Check ClickUp and Cursor+Agent to confirm which domain is next in the curriculum sequence per Subject Roadmap A. Select the domain with the blocking task cleared (or the next scheduled domain if no prior tasks are incomplete). OUTPUT: Named domain and clear intent for this build ("Next domain: Healthcare logistics agents"). NOT choosing a domain based on urgency or interest alone. | (1) Open ClickUp workspace and review curriculum milestone tasks. (2) Check: is there a blocking task (e.g. "Domain A complete") that has not yet been marked done? If yes — work on that blocking task before starting any new build. (3) If no blocking tasks: check Subject Roadmap A for curriculum commitment (which cross-domain practice is scheduled next). (4) Select the next scheduled domain. (5) Confirm with ILE Learning Agent in Cursor: "Next domain per A is [X]. Correct?" Agent confirms or corrects. Gate: domain is selected and confirmed. | Curriculum commitment in Subject Roadmap A + ClickUp task blocking — an external structural contract that specifies which domain to build in next, independent of Learner's moment-to-moment urgency. | Written commitment + operational task structure → Learner cannot argue themselves into the comfort domain → domain selection follows a plan, not a feeling → builds happen across domains in sequence → cross-domain pattern exposure accumulates. | P12(Sc): "Write explicit multi-domain curriculum commitment in Subject Roadmap A and bind yourself to the sequence; ILE Learning Agent and ClickUp milestones track it visibly." P11(S): "Install structured forcing mechanism (Agent or task system) before solo learning begins." | ClickUp (WORKSPACE.3); Cursor IDE with ILE Learning Agent (WORKSPACE.2); Subject Roadmap A (state reference). | Curriculum commitment written in A and visible to Agent. ClickUp tasks configured with blocking dependencies. Learner treats Agent's domain recommendation as binding. | Domain selection is skipped — Learner starts building without checking curriculum. Learner checks ClickUp but ignores the blocking structure ("I'll do the other domain later"). Learner picks Investment domain every time because it feels more relevant. Agent recommendation is ignored. | Urgency bias — the Learner's sense that "this Investment build is more important right now" overrides the curriculum commitment. Without a structural external gate, urgency always wins over sequence. | Urgency → Learner selects comfort domain → builds accumulate in one domain → pattern blindness deepens → when the Learner finally tries another domain, it feels completely new → no cross-domain skill transfer → compounding fails. | "I can build cross-domain understanding by just studying examples." Active building forces decomposition decisions that study cannot; you must build in the domain, not just read about it. "My Investment domain work is more urgent." Urgency and curriculum sequence are in conflict; curriculum sequence must win for the system to work. | No ClickUp configuration. A not updated. Agent recommendation not visible. | High-urgency project environment where Investment work always feels more important. No external accountability (solo learner with no checks). | If ClickUp is not configured: open Subject Roadmap A, read curriculum commitment, select the written next domain manually. Acknowledge the absence of structural enforcement and ask the ILE Learning Agent to remind you of the commitment at session start. The goal is any external gate — written in A, confirmed by Agent, or tracked in ClickUp. One of the three is sufficient. | STEP.2: Execute a bounded agent build in the selected domain. |
| **STEP.2: Execute a Bounded Agent Build** | Enables UDS.UD: Deliberate post-build reflection requires a build to reflect on. Without a completed build, extraction has no signal — no real decomposition decision was made, no pass/fail occurred, no context exists to reflect on. The build is the raw material for principle extraction. | Execute one focused Agno agent build in the domain selected in STEP.1. The build must test one specific decomposition boundary (e.g., whether two roles should be one agent or two; whether a handoff should be sequential or parallel). OUTPUT: A completed build — agent runs or fails. Either outcome generates reflection signal. NOT a research session, planning session, or tutorial; an execution session where code runs. | (1) Open Cursor IDE in this subject's workspace. (2) Define the decomposition boundary to test: "I want to know if this domain's handoff should be sequential (Research → Risk) or parallel (both simultaneously)." Write it down (Session Log or comment in code). (3) Build the agent with Python + Agno: define agents, tools, instructions, team/workflow structure. (4) Run the build: `python agent.py`. (5) Observe outcome: does it run? What happened at the boundary? Note pass/fail and any unexpected behavior. Gate: build runs or fails with a clear outcome. Both outcomes are valid — a failure generates at least as much signal as a success. | Commitment to hands-on building (running actual code, not reading) + a bounded scope (one boundary per build) that makes the decomposition decision observable in the output. | Bounded scope → one specific boundary is tested → when the build runs, the boundary decision is observable (e.g., latency changed; context was lost; handoff worked cleanly) → outcome is specific and recent → reflection has concrete material → principle derivation is grounded. | P3(E): "Extract principle after every build — extraction requires a build first." P11(S): "Install structured mechanism before solo learning begins — builds must be executed, not just planned." | Python 3 Runtime (INFRA.1); Anthropic API + Claude LLM (INFRA.2); Cursor IDE (WORKSPACE.2). | Python environment running (PATH configured; packages installed). Agno installed (`pip install agno`). Anthropic API key set. One bounded scope defined before starting. | Build is started but abandoned mid-way (Learner is unsure and stops before running). Build runs but the scope is too broad ("I tested everything at once") — no single boundary is observable. Build "fails" and Learner concludes the session is wasted — does not extract principle from the failure. Build runs but Learner does not record the outcome before moving on. | Scope indiscipline — testing too many boundaries at once makes it impossible to attribute outcomes to a specific decision. Alternatively, avoidance of running (endless planning without execution). | Broad scope → build runs → outcome is ambiguous ("something worked but I'm not sure what") → reflection has no specific signal → principle extraction is vague ("decomposition is hard") → no learning transfers to next build. | "The build has to succeed for me to learn from it." False — a failure with a clear cause is a better learning signal than a success with an unclear cause. "I need to plan more before running." Planning is not building; STEP.2 must end with code running. | Python environment broken. API key not set. Scope not defined. | No time for a full build. Too many competing tasks. Environment not set up. | If the build is blocked by environment (Python not running, API key missing): fix environment first (see T2.P4 INFRA.1 and INFRA.2 resolution steps). If scope is unclear: narrow it to one question ("Does sequential or parallel handoff reduce latency for this domain?"). Run the build with that one question only. Do not defer to a research session. Even a 30-minute build generates reflection signal. | STEP.3: Open extraction template immediately post-build. |
| **STEP.3: Open Extraction Template Immediately Post-Build** | Disables UDS.UD.UB: Reflection bypass. The bypass occurs when the Learner finishes a build and moves to the next task without extracting the principle — because extraction feels like extra work when urgency is high. Opening the template immediately, before any other task, installs a structural barrier to the bypass. | Immediately after the build (same session, within minutes of STEP.2 completing), open ILE Learning Book → Session Log and paste the extraction template: "Decision: __; Outcome: __; Rule: __; Challenge: ☐". DO NOT close Cursor or switch context until the template is open. OUTPUT: Template is visually open with three empty fields, waiting to be filled. NOT a mental note ("I'll remember to write this later"). NOT deferred to a separate "reflection session." | (1) Build completes (pass or fail) in STEP.2. (2) Immediately — without switching windows — navigate in Cursor to ILE Learning Book → target page Session Log (or T2.P3 annotation space). (3) Paste extraction template: "Decision: __ ; Outcome: __ ; Rule: __ ; Challenge: ☐ ". (4) Do not fill it yet — just open it. The act of opening is the step. Gate: template is visually open and waiting. | Structural proximity — the template is in the same IDE window as the build, so the cost of opening it is near-zero. Proximity defeats the bypass: bypassing requires more effort than complying. | Near-zero cost → opening template feels trivially easy → Learner opens it → once open, the blank fields create cognitive pressure to fill them → closure instinct drives completion → extraction is now more likely than skip. | P15(S): "Principle written in fixed format (Decision; Outcome; Rule) with gate blocking next cycle until complete — open template is the beginning of the gate." P3(E): "Extract principle in same session, before next cycle opens — opening template starts the same-session clock." | ILE Learning Book (WORKSPACE.1); Cursor IDE (WORKSPACE.2); Principle Extraction Template (INTEL.3). | Template is already in ILE Learning Book (not something Learner has to remember to create). Cursor IDE is the primary window — both code and ILE Book are accessible without switching apps. | Build completes and Learner immediately closes Cursor and goes to a meeting. Build completes and Learner defers ("I'll open the template after lunch"). Template is not pre-existing — Learner has to create it from scratch each time (friction too high). | Urgency pull — another task, meeting, or context switch immediately after the build pulls the Learner away before the template is opened. Without a pre-existing template, the friction of creating it from scratch adds just enough resistance to enable the bypass. | Urgency → Learner closes IDE → context is lost (code is not running; build state is not visible) → extraction is now deferred → deferred extraction requires memory reconstruction → memory reconstruction degrades precision → principle is vague → compounding stalls. | "I'll write the principle later when I have more time." Later has no build context; context degradation is fast. After 2 hours, the boundary you tested is no longer fresh. "I'll remember the key insight." Memory is not the same as written precision; even 10 minutes later, nuance is lost. | Template not pre-created in ILE Book. Cursor is closed immediately after build. Context switch (meeting, Slack message) immediately after build. | No quiet window after the build. Team environment with immediate follow-up demands. | If the context switch is unavoidable (meeting immediately after build): open template and write one word under "Decision:" before leaving. Literally write one word. The act of writing something is the gate — it commits the context to paper and signals the session is not complete. Return within the same day to complete STEP.4–6 while context is still partially fresh. | STEP.4: Fill extraction template (Decision → Outcome → Rule). |
| **STEP.4: Fill Extraction Template (Decision → Outcome → Rule)** | Enables UDS.UD: Deliberate post-build reflection. Filling the template is the core act of reflection — converting the build experience into explicit, transferable knowledge. Without completing this step, the build remains isolated experience; with it, the experience becomes a principle that compounds with future builds. | With the template open (from STEP.3), fill in all three fields: (1) Decision: what boundary was tested in this build? What specific variable was changed or tested? (2) Outcome: what happened as a result — pass or fail? What was observed at the boundary? (3) Rule: if [condition], [result] occurs. Apply rule: [action for next build]. OUTPUT: All three fields completed, in precise language. NOT a journal entry (no narratives). NOT vague ("decomposition is important"). Each field must be one to three sentences, specific and testable. | (1) Decision field: name the boundary tested. "Decision: Tested whether Research and Risk agents could share state or needed separate outputs. Changed: whether Risk agent depends on Research agent's output directly vs receiving a formatted summary." (2) Outcome field: describe the specific result. "Outcome: Sequential (direct state dependency) caused 2-second delay at handoff; parallel (formatted summary) had no delay. Decoupled version passed; coupled version had synchronization overhead." (3) Rule field: write as a conditional. "Rule: When agent outputs are narrative (formatted summaries, not raw state), decouple agents. Condition: narrative handoff. Action: separate agents and pass formatted output, not raw state." Gate: three fields completed, all in specific conditional language. | Prior build context still fresh in the session — Learner can recall exactly which boundary was tested, what the outcome was, and what the rule should be, because the build just happened minutes ago. Freshness is the fuel for precision. | Freshness → recall is specific → boundaries are named precisely (not "something about handoffs") → outcome is attributed to the specific decision ("sequential caused 2-second delay, not other factors") → rule is causal, not correlational → principle is precise and testable. | P3(E): "Conscious extraction converts doing into knowing — extract after every build while context is fresh." P15(S): "Principle written in fixed format (Decision; Outcome; Rule) with gate blocking next cycle until complete." | Principle Extraction Template (INTEL.3); ILE Learning Book (WORKSPACE.1); Session context (working memory — the Learner's own recent build memory). | Build is still running or just completed (context is fresh). Template is already open from STEP.3. Learner can access the code or output (still visible in terminal or notebook). | Rule field is vague ("Decomposition is important"). Decision field is too broad ("I tested the team architecture"). Learner starts writing but stops because they cannot articulate the rule precisely. Template is filled but in prose narrative format (not the Decision; Outcome; Rule structure). | Vague language — without structure, the Learner writes what feels right rather than what is causal. Prose narrative is easier to write than conditional rules, so Learner defaults to narrative without the format constraint. | Vague language → rule is not conditional ("decomposition is important" is not a rule — there is no condition and no action) → next build cannot apply the rule because it is not specific enough → extraction does not compound → skill stays implicit. | "I can't articulate the rule clearly." This is the most important signal: inability to articulate means the principle is not yet understood. In that case, write what you observed: "I don't know why [outcome] happened, but I noticed [observation]." That is enough — the principle will clarify after another build. "The format is too rigid." The format is rigid by design; precision requires constraint. | Template is not structured (Learner free-writing). Build output not visible (context already closed). | Solo learning with no format enforcement. Learner has not practiced the template format enough for it to feel natural. | If the rule cannot be articulated: write the Decision and Outcome only. Leave Rule as: "Unknown — need another build." Mark Challenge checklist as "Not yet challenged." Commit the partial extraction anyway. A partial principle that is written and committed is more useful than a perfect principle that is never written. Return to the Rule field after the next build when the pattern repeats. | STEP.5: Challenge the principle with ILE Learning Agent. |
| **STEP.5: Challenge the Principle with ILE Learning Agent** | Enables UDS.UD.UD: Structured same-session extraction gate. Unchallenged principles are vague rules written from a single data point ("decoupling is better"). The challenge forces the Learner to ask: "Is that the real reason?" and "What would falsify this?" — sharpening the rule from experience to principle. Without challenge, the principle stays at experience-level, not principle-level. | With the rule written (from STEP.4), open ILE Learning Agent in Cursor and submit the extraction for challenge: "Challenge my principle: [Rule]. Is that the real reason? What would falsify it?" Listen to the Agent's response. Revise the Rule if the challenge reveals a gap. OUTPUT: Principle is challenged and either confirmed (Agent's challenge is answered satisfactorily) or revised (Rule is updated with new condition or qualifier). NOT a conversation where the Agent just says "looks good." | (1) Copy the Rule from STEP.4 into Cursor chat. (2) Ask: "Challenge my principle: [Rule]. Is that the real reason? What would falsify it?" (3) Agent responds with a challenging question (e.g., "What if the outcome was caused by network latency, not decoupling? How would you falsify that?"). (4) Learner responds to the challenge: either (a) the Rule stands ("The build isolated latency — same network conditions, only structural change was decoupling") or (b) the Rule must be revised ("You're right — I didn't control for network latency. Revised rule: when structural decoupling is the only variable changed and latency decreases, decouple."). (5) Write revised Rule back into the template. Gate: challenge is answered — either rule stands or rule is revised. | Access to ILE Learning Agent (Claude LLM in Cursor) + Learner's willingness to accept and act on the challenge. The Agent's access to CLAUDE.md and ILE context makes challenges specific to this Topic and this principle, not generic. | Agent context-awareness → challenge is specific ("Is this a decomposition principle or a latency principle?") → Learner must answer specifically → specificity forces clarification → rule is revised or confirmed at a more precise level → principle is sharpened. | P16(E): "Challenge extracted principle in same session — 'Is that the real reason? What would falsify it?' — while context is fresh and revision is possible." | ILE Learning Agent (INTEL.1); Anthropic API + Claude LLM (INFRA.2); Principle Extraction Template (INTEL.3 — the rule to challenge). | ILE Learning Agent is active in Cursor (CLAUDE.md loaded; A in scope). Learner submits the challenge request explicitly — Agent does not challenge automatically; Learner must ask. Rule from STEP.4 is specific enough to challenge (vague rules cannot be challenged meaningfully). | Learner skips the challenge ("The rule seems right"). Learner challenges but accepts the first revision from the Agent without deeper questioning. Agent is not available (CLAUDE.md not loaded; API not accessible). Rule from STEP.4 is too vague to challenge ("decomposition is important" — no condition to falsify). | Self-certainty — the Learner believes their first formulation is correct and does not want it challenged. Without asking for challenge, no challenge comes. | Self-certainty → rule is not challenged → principle stays at first-order experience level → when applied to next domain, it fails because it was not tested against alternative explanations → Learner gets confused ("the principle didn't work") → trust in extraction breaks down. | "The Agent's challenge might confuse me." Confusion from challenge means the principle needs revision — the confusion is the signal. "My principle is already good enough." A principle that survives challenge is stronger than one that was never challenged; always challenge. | CLAUDE.md not loaded. API not accessible. Rule too vague to challenge. | No time for challenge. Solo learning with no feedback mechanism. | If Agent is not available: self-challenge using the falsification question: "What would have to be true for this rule to be wrong? Under what conditions would decoupling NOT reduce latency?" Write the answer in the template under "Challenge." Even self-challenge is better than no challenge. If Rule is too vague to challenge: go back to STEP.4 and make it more specific. Rule must be conditional before it can be challenged. | STEP.6: Commit refined principle to ILE Learning Book (Git). |
| **STEP.6: Commit Refined Principle to ILE Learning Book (Git)** | Disables UBS.UD.UB: Reflection bypass. The bypass is fully disabled only when the principle is committed to Git — not just written, not just challenged, but persisted in version control. Git commit is the structural gate: the next build cycle cannot open until the commit is complete. Without the commit, the principle exists only in a local file that can be lost, overwritten, or forgotten. | With the refined principle in the template (after STEP.5), write it to the target page file in ILE Learning Book and commit to Git: `git add {file}; git commit -m "T2: principle [X] extracted from [domain] build [date]"`. OUTPUT: A Git commit with a timestamp-stamped principle, versioned in the Learning Book. NOT a principle that lives only in Cursor chat history. NOT a principle written in a notes app, Slack, or email. NOT a file change without a Git commit (file can be lost). | (1) Write the refined Rule (from STEP.5) into the target ILE page file — e.g., Session Log entry in A, or annotation row in T2.P3. Format: "Decision: [X]; Outcome: [Y]; Rule: [if C then A]; Challenge: ✅ challenged; Revised: [if revision]." (2) Stage and commit: `git add learning-book/...{file}; git commit -m "T2: [principle name] — extracted [date], challenged, revised."` (3) Confirm commit: `git log --oneline -1` — new commit appears at top. Gate: `git log` shows new commit; principle is in version control. | Structural permanence of Git — once committed, the principle has a timestamp, an author, and a hash; it cannot be accidentally deleted or forgotten. Version control is the structural backbone of cumulative principle storage. | Git commit → principle has permanent record → next session, Learner can `git log` and find the principle → applies to next build → compounding begins → skill is not linear (every build adds to a growing body of principles). | P3(E): "Extract principle in explicit language after every build — commit to ILE Book closes the extraction loop." P15(S): "Principle written in fixed format with gate blocking next cycle until complete — Git commit is the gate signal." | ILE Learning Book (WORKSPACE.1); Git (part of WORKSPACE.1); Terminal in Cursor IDE. | Git is initialized in learning-book repository. Learner has write access to commit. Target page file is in correct location and named correctly. | Git commit is skipped — principle is written in file but not committed. Commit message is generic ("update notes") — principle cannot be found by future searches. File is written in wrong location (not in ILE Learning Book). Principle is committed but the Rule field is still vague (challenge was skipped in STEP.5). | No Git commit discipline — Learner writes the principle but does not commit, believing the file will persist. Local file can be lost in a machine reset or accidental deletion. | No commit → principle not versioned → if machine resets or file is overwritten, principle is lost → future builds cannot reference prior principles → compounding fails → skill stays episodic (each build isolated). | "I'll commit later when I have more to commit." Commit now — the principle is the valuable artifact, not the code. Even one-line commits are valuable. "Git is overhead." 30-second overhead per cycle is the entire cost of permanent knowledge storage. | Git not initialized. No terminal access for commit. Wrong file location. | Corporate policy forbidding local Git repos. No terminal access in IDE. | If Git is not available: write principle to a cloud-synced file (Google Drive or GitHub web) and mark the date and build explicitly. The goal is timestamp-stamped, backed-up permanence — Git is the ideal vehicle, but any timestamped, backed-up storage is better than nothing. After session, initialize Git (`git init`) and commit. Make Git setup a prerequisite for the next cycle. | STEP.7: Cross-domain comparison + curriculum update. |
| **STEP.7: Cross-Domain Comparison and Curriculum Update** | Disables UBS (Pattern Blindness) — the root UBS of Topic 2. Pattern blindness is the assumption that each domain's decomposition is unique; it persists until the Learner explicitly sees the same pattern in two or more domains side-by-side. This step forces the comparison and updates the curriculum milestone to close the gate. | After committing the principle from the current domain build, compare it to the principle(s) from a prior domain build: "In Domain A, the rule was [X]. In Domain B (this build), the rule is [Y]. Are they the same pattern? Same condition, same action, different surface details?" Document the comparison in 1–2 sentences. Then mark the curriculum milestone in ClickUp complete and update A (Session Log). OUTPUT: A written cross-domain pattern note ("Both Finance and Healthcare decompose by narrative handoff role") and a ClickUp milestone marked complete. | (1) Open INTEL.2 (Agno reference architectures or prior build notes from Domain A). (2) Read Domain A principle. (3) Place Domain A and Domain B principles side by side: "Domain A rule: [X]. Domain B rule: [Y]." (4) Ask: "Same condition? Same action? Same structural pattern?" Write the answer: "Pattern: both decompose by specialist role when outputs are narrative and one-directional. Domain varies (Finance vs Healthcare); pattern is invariant." (5) Open ClickUp (WORKSPACE.3) and mark the cross-domain milestone complete ("Domain B build + comparison complete"). (6) Update Subject Roadmap A Session Log. Gate: comparison is written; ClickUp milestone is marked complete; A is updated. | Two built-and-extracted domains side by side — the comparison is only possible after STEP.2–6 are complete for at least two domains. The pattern is only visible when both data points exist. | Two-domain data → comparison is concrete (not hypothetical) → pattern is either confirmed ("same structure") or refined ("same structure but different trigger condition") → cross-domain principle is written → pattern blindness is explicitly broken for this decomposition dimension → next domain build is framed differently ("I'm looking for the same pattern again"). | P9(E): "Decompose 2+ completely different domains back-to-back with explicit comparison before deep single-domain mastery — prevents pattern blindness." P12(Sc): "Write curriculum commitment in A and bind yourself to the sequence; ClickUp milestones track it visibly — update milestones after each domain cycle." | Agno Reference Architectures (INTEL.2); ClickUp Task Tracking (WORKSPACE.3); ILE Learning Book (WORKSPACE.1 — prior domain principles to compare). | At least two domain builds have been completed and principles extracted (STEP.2–6 done for Domain A and Domain B). Domain A principle is written and committed in ILE Learning Book. ClickUp curriculum milestone for Domain B is defined. | Comparison is skipped — Learner marks ClickUp complete without writing the pattern note. Pattern note is written but vague ("both had agents talking to each other"). ClickUp is not updated — curriculum state is stale. A is not updated — next session Agent has incorrect state. | No comparison habit — the Learner extracts principles per domain but never places them side by side to look for the universal pattern. The comparison is the final act that breaks pattern blindness; without it, each domain remains isolated in the Learner's mental model. | Domain isolation → principles are per-domain → Learner still believes each domain is unique → next domain build still starts from scratch → pattern blindness persists → cross-domain skill transfer does not occur → compounding stalls. | "The pattern is obvious — I don't need to write it." Writing the pattern is not for when it is obvious; it is for when it must be applied in an unfamiliar domain in 6 months when the present context is gone. "I'll update ClickUp later." Update now; ClickUp state is only useful when current. | No Agno references for Domain A. ClickUp not configured. Prior domain principles not committed (nothing to compare to). | Only one domain build completed (cannot compare). ClickUp not accessible. | If only one domain build has been completed (Domain A done; Domain B not yet): STEP.7 is deferred until Domain B is complete. Proceed to STEP.1 for Domain B now. When Domain B is complete and STEP.6 is done, return to STEP.7 to complete the cross-domain comparison. The comparison requires both data points — do not force it with one. | CYCLE COMPLETE. Return to STEP.1 with next domain per curriculum. After N cross-domain cycles (per A Level→Scope), check if L2 scope is met (T2.P2–2.2 approved; L2 learning gates cleared). |

---

*7 steps · Sequential and gated — each step's gate must clear before the next step opens.*
*All 6 principles operationalised: P3 (STEP.2–4, 6); P9 (STEP.7); P11 (STEP.1–2); P12 (STEP.1, 7); P15 (STEP.3–4, 6); P16 (STEP.5).*
*All 8 components active: INFRA.1-2 (STEP.2); WORKSPACE.1 (STEP.3–4, 6–7); WORKSPACE.2 (STEP.1, 3, 5); WORKSPACE.3 (STEP.1, 7); INTEL.1 (STEP.5); INTEL.2 (STEP.7); INTEL.3 (STEP.3–5).*
*Topic 2 (UDS / Hierarchical Decomposition Thinking via Deliberate Post-Build Reflection) is now a complete system: P0 (Overview) + P1 (UB layer) + P2 (UD layer) + P3 (Principles) + P4 (Components) + P5 (EOP). 18 of 36 pages done (T0: 6, T1: 6, T2: 6).*
