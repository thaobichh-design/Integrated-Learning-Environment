# Topic 0. Overview & Summary — Page 5: Steps to Apply
*Phase C. Organise Information | Topic: 0. Overview & Summary | Page: 5. Steps to Apply*
*Subject: AI Orchestration / Engineering | UDO: Build and master an OE System that any analyst/operator can use to build domain-specific UE systems. Effective = Sustainable, Efficient, Scalable.*
*Derives from: T0.P1 (UBS), T0.P2 (UDS), T0.P3 (P1/P2/P3), T0.P4 (components). Sequential and gated — each step must be complete before the next begins.*

---

## Column Key

| # | Group | Question |
|---|---|---|
| 1 | INTRODUCTION | What is it for? Why is it important? (Relevance) |
| 2 | INTRODUCTION | What is it precisely — and what is it NOT? (Precise Definition) |
| 3 | SUCCESS | How does it work successfully? (Effective Operating Procedure) |
| 4 | SUCCESS | What ultimately causes it to work successfully? (Ultimate Driving System) |
| 5 | SUCCESS | How does the UDS cause it to work successfully? (Success Mechanism) |
| 6 | SUCCESS | What principles is the UDS based on? (Effective Principle System) |
| 7 | SUCCESS | What tools do the ultimate drivers require? (Ultimate Effective System) |
| 8 | SUCCESS | What environmental conditions do the ultimate drivers require? (Ultimate Effective System) |
| 9 | FAILURE | How can it fail? (Failure Actions) |
| 10 | FAILURE | What ultimately causes it to fail? (Ultimate Blocking System) |
| 11 | FAILURE | How does the UBS cause it to fail? (Failure Mechanism) |
| 12 | FAILURE | What principles are the blockers based on? (Risky Principles) |
| 13 | FAILURE | What tools do the blockers require? (Risky Tools) |
| 14 | FAILURE | What environmental conditions do the blockers require? (Risky Environments) |
| 15 | LEARNER'S NOTE | What to do if it fails? (What else?) |
| 16 | LEARNER'S NOTE | Next Steps to Take (Now What? Now How?) |

---

## Gate Logic
Each step has an explicit gate. The gate must be passed before the next step begins. Advancing without meeting the gate is a P2 violation — it breaks the Acceptance Criteria (A.C.) principle.

---

## Table

| Row | 1 · Relevance | 2 · Precise Definition | 3 · EOP | 4 · UDS | 5 · Success Mechanism | 6 · EPS | 7 · Tools (UES) | 8 · Environment (UES) | 9 · Failure Actions | 10 · UBS | 11 · Failure Mechanism | 12 · Risky Principles | 13 · Risky Tools | 14 · Risky Environment | 15 · What Else? | 16 · Next Steps |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **STEP.1 · Confirm all components** *(Activates: T0.P4 all)* | No step of the EOP can run without the full component stack confirmed. An unconfirmed component creates silent failures that masquerade as System Design problems — wasting loop cycles on the wrong diagnosis. Confirming all components first is the zero-waste gate. | A systematic, documented confirmation pass through all 7 T0.P4 components before any ELF or build work begins — each component tested operationally, not merely assumed installed. IS the zero-waste gate: an unconfirmed component creates silent failures that masquerade as System Design problems, wasting P3 loop cycles on the wrong diagnosis. IS NOT a one-time setup pass skippable on repeat sessions. IS NOT 'check if software is installed': each component requires its actual confirmation test (hello-world run, API ping, Git log, structured output return, event stream output) with each pass recorded. | Run each confirmation test in order: INFRA first, then WORKSPACE, then INTEL. Record each pass in the ILE Learning Book component confirmation log. **Gate:** all 7 components confirmed and recorded before STEP.2 begins. Do not open the ELF session until this gate is passed. | Systematic confirmation — the discipline to test each component in isolation before combining them. The same tool isolation principle from P2 applied at the infrastructure level: if you confirm all components individually, any future failure must originate from the System Design, not the stack. | Systematic confirmation → all 7 components known-good → when STEP.2 begins, the user can attribute any failure to the design work, not the environment → P3 causal traces are clean from the first build → loop efficiency is maximum from day one. | P2 (hypothesis-driven build): the Acceptance Criteria (A.C.) for STEP.1 is "all 7 components pass their confirmation test." No ambiguity. No "good enough." Either all pass or STEP.2 does not begin. | INFRA.1 hello-world script, INFRA.2 API ping script, WORKSPACE.1 CLAUDE.md, WORKSPACE.2 Git log, WORKSPACE.3 ILE Learning Book template, INTEL.1 structured output test, INTEL.2 event stream test. All located in T0.P4. | One uninterrupted session allocated to STEP.1 alone. No pressure to "start building" before all 7 gates pass. Internet access for pip installs and API calls. | User skips STEP.1 and begins STEP.2 immediately → a broken component (e.g., wrong Python environment) fails silently mid-build → user diagnoses it as a System Design error → incorrect P3 trace → wrong thing revised → loop wasted. | Impatience — the user wants to start building agents, not confirming environments. STEP.1 feels like overhead when the real work ("building") hasn't started yet. | Impatience → STEP.1 skipped → broken component silently corrupts STEP.2, STEP.3, or STEP.4 → failures are ambiguous → P3 traces are inaccurate → incorrect revisions sent back to P1 → multiple loop cycles wasted on what is actually a 30-minute setup problem. | "I'll fix the environment if something breaks." Environment problems are the hardest failures to diagnose mid-build because they manifest as agent failures, not infrastructure failures. Fixing them after they've caused confusion costs 10x more than preventing them upfront. | Jumping directly into Cursor and writing agent code without running confirmation tests. Treating "I installed Agno last week" as equivalent to "INFRA.1 is confirmed today." | No STEP.1 confirmation checklist. No record of which components were confirmed and when. No personal rule that STEP.2 cannot begin until all 7 pass. | If a component fails during STEP.1: fix that component before testing the next. Do not continue the confirmation sequence with a broken component. A broken INFRA.1 means INFRA.2 cannot be meaningfully tested. | STEP.1 gate: all 7 components confirmed and recorded in ILE Learning Book. Then STEP.2 begins. |
| **STEP.2 · Apply Effective Learning Framework → produce System Design for first agent** *(Activates: P1, WORKSPACE.1, WORKSPACE.3)* | This is the design act. Before any code is written, the user applies the Effective Learning Framework to the first agent as a learning subject. The output is the System Design — the causal map of the agent's job. Without this, all subsequent steps build the wrong thing correctly. | The design act: applying ELF's 7 layers (UDO → Roles → UBS → UDS → EPS → UES → EOP) to the target agent in the ILE Learning Book — producing a written System Design, the causal map that P2 and P3 will act on. IS the entire design work for this agent: code in STEP.4 implements this design, adding nothing. IS NOT optional pre-work: it is the design. IS NOT complete if any layer is answered vaguely — the ILE Learning Agent must challenge each answer until causally precise. Cursor is not opened before this step's gate is passed. | Run each ELF layer in order. Do not skip to UES before UBS and UDS are resolved — the component choices depend on the causal structure. Use the ILE Learning Agent to challenge each answer: "Is this the real UBS or a symptom?" **Gate:** all 7 ELF layers answered and recorded in the ILE Learning Book before STEP.3 begins. Cursor is not opened until this gate passes. | The ILE Learning Agent as sparring partner — actively challenging each ELF answer forces the user to defend or deepen their causal thinking. An unchallenged ELF answer is often a surface answer. A challenged and defended answer is a genuine design decision. | Sparring → each ELF answer is tested for causal validity → weak answers are exposed before they become design flaws → the resulting System Design is causally grounded → the agent built from it is far more likely to pass Acceptance Criteria (A.C.) on the first test → fewer loop cycles needed to converge. | P1 (ELF → System Design): this step IS the application of P1. The System Design produced here is what P2 (Acceptance Criteria (A.C.)) and P3 (causal trace) will act on. A weak System Design at STEP.2 creates cascading inaccuracies in STEP.3, STEP.5, and STEP.6. | ILE Learning Book (System Design template), ILE Learning Agent (sparring partner via CLAUDE.md), Effective Learning Framework questions (UDO → UBS → UDS → EPS → UES → EOP). | Quiet, uninterrupted session allocated to ELF work. No Cursor open during STEP.2. ILE Learning Agent active. A personal rule: if you cannot answer a UBS or UDS question, you are not ready to build the agent — research first. | User rushes through the ELF surface-level, writes vague answers ("UBS: not knowing how to build agents"), and declares the System Design complete → proceeds to STEP.3 with a shallow causal map → Acceptance Criteria (A.C.) at STEP.3 are not grounded in the real agent structure → the build produces the wrong agent → Acceptance Criteria (A.C.) fail → user cannot trace why. | Surface-level ELF — the user fills in the framework fields with the first answers that come to mind rather than the causally deepest ones. The ELF looks complete but lacks the causal depth that makes it useful as a design specification. | Surface ELF → System Design is a shallow map → Acceptance Criteria (A.C.) at STEP.3 test the wrong things → agent is built to a weak spec → fails in ways that cannot be accurately traced → P3 causal trace points to "the System Design was wrong" but cannot identify which specific element → loop cannot converge. | "I understand this agent well enough to build it — I don't need to run the full ELF." If you could build the correct agent without the ELF, you would not need the framework. The ELF is the tool for surfacing what you do not yet know about the agent's causal structure. | Any note-taking tool that accepts vague answers without challenging them. Running the ELF mentally rather than in writing (written answers are more testable than mental ones). | No ILE Learning Agent active (CLAUDE.md not loaded). No sparring partner to challenge surface answers. Time pressure that forces a "quick" ELF. | If the System Design feels weak after completing STEP.2: do not proceed to STEP.3. Return to the ELF and challenge the weakest layer again. Ask the ILE Learning Agent: "What UBS am I missing?" Better to spend another hour on STEP.2 than to build to a wrong design. | STEP.2 gate: all 7 ELF layers answered in writing, System Design recorded in ILE Learning Book, ILE Learning Agent has challenged at least one answer. Then STEP.3 begins. |
| **STEP.3 · Define hypothesis and Acceptance Criteria (A.C.) from the System Design** *(Activates: P2, WORKSPACE.2, WORKSPACE.3)* | The hypothesis and Acceptance Criteria (A.C.) convert the System Design from a design document into a testable proposition. Without them, the build in STEP.4 has no objective endpoint — it ends when the user feels done, not when the System Design is validated. This is what makes the EOP hypothesis-driven rather than intuition-driven. | Two items written and locked in the ILE Learning Book before Cursor opens: (1) a one-sentence hypothesis ('If this System Design is correct, the agent will produce [X] on input [Y]'), and (2) a binary pass/fail A.C. checklist derived from the System Design's UES output schema. IS the pre-commitment mechanism that makes the STEP.5 decision immune to post-result rationalisation. IS NOT written after seeing early test results: post-result A.C. is unconsciously shaped by the result. IS NOT qualitative ('good output'): each criterion must be specific, measurable, and testable against real domain data with a binary outcome. | Write the hypothesis in one sentence. Write the Acceptance Criteria (A.C.) as a checklist of specific, binary pass/fail conditions. Confirm each criterion is testable against real data (not mocked). Record both in the ILE Learning Book. **Gate:** hypothesis and Acceptance Criteria (A.C.) written and locked before Cursor opens for STEP.4. Once locked, the criteria cannot be softened post-result. | Pre-commitment discipline — the act of writing Acceptance Criteria (A.C.) before building makes them binding. Criteria written after seeing the result are unconsciously shaped by the result. Criteria written before the build are objective. Pre-commitment is the mechanism that makes STEP.3 meaningful. | Pre-commitment → Acceptance Criteria (A.C.) are objective → the STEP.5 test is unambiguous (pass or fail) → the user cannot rationalise a partial result as "done" → each phase closes with verified evidence → P3 causal traces at STEP.6 point to specific criteria, not vague impressions of quality. | P2 (hypothesis-driven build): STEP.3 IS the application of P2's pre-commitment discipline. The hypothesis is the proposition being tested. The Acceptance Criteria (A.C.) are the pass/advance decision boundary set before any sunk cost is incurred. | ILE Learning Book (hypothesis and Acceptance Criteria (A.C.) section), WORKSPACE.2 Git (the Learning Book entry with locked criteria will be committed at STEP.4's gate). | Kill criteria written in the same ILE session as STEP.2 — not in a separate session days later. A personal rule: once the Acceptance Criteria (A.C.) are recorded, they cannot be weakened. They can only be invalidated entirely (forcing a return to STEP.2) if the System Design itself changes. | User writes vague Acceptance Criteria (A.C.) ("the agent should produce a useful output") → STEP.5 test is subjective → user declares the agent "good enough" when it produces any output → proceeds to STEP.6 with no real validation → P3 causal trace is meaningless because there was nothing specific to trace back to. | Criterion vagueness — writing Acceptance Criteria (A.C.) in qualitative language rather than specific, measurable terms. "Good output" is not a kill criterion. "Returns a Pydantic object with fields [revenue_growth, confidence_score] both non-null when queried with ticker='AAPL'" is a kill criterion. | Vagueness → STEP.5 test is subjective → every result looks like a pass to some degree → the pass/advance decision becomes a mood decision → some phases advance with incomplete validation → foundation weaknesses carry forward → system becomes fragile. | "I'll know a good output when I see it." Output quality judgment is unreliable and inconsistent across sessions and energy levels. Binary Acceptance Criteria (A.C.) are reliable. | Writing Acceptance Criteria (A.C.) after seeing early test results. Criteria that reference "quality" or "usefulness" rather than specific fields, values, or structures. | No ILE Learning Book template with a dedicated Acceptance Criteria (A.C.) section. No personal rule against weakening criteria post-result. | If Acceptance Criteria (A.C.) feel impossible to make specific: return to STEP.2. The inability to write specific Acceptance Criteria (A.C.) is a signal that the System Design's UES layer (what the agent produces) is not specific enough. Fix the design, then return to STEP.3. | STEP.3 gate: hypothesis written in one sentence, Acceptance Criteria (A.C.) written as a binary pass/fail checklist, both recorded in ILE Learning Book and committed to Git. Then STEP.4 begins. |
| **STEP.4 · Build the agent from the System Design** *(Activates: INTEL.1, INFRA.1, INFRA.2, WORKSPACE.1, WORKSPACE.2)* | This is the implementation step — and the shortest. The System Design from STEP.2 and the Acceptance Criteria (A.C.) from STEP.3 mean the build is constrained to one specific target. There is no design work to do here, only translation from System Design to code. | The pure implementation step: translating the STEP.2 System Design into an Agno agent definition in Cursor using Claude AI assistance — no design decisions, only translation. IS a translation act: the design is complete before this step; STEP.4 only implements it. IS NOT the design act: if a gap in the System Design is discovered here, the correct response is to stop and return to STEP.2, not invent a solution mid-build. IS NOT complete until the code is committed to Git with a descriptive message (not just saved — committed, confirming what was built and that stream_events=True is active). | Translate System Design layer by layer into the Agno Agent constructor: UES tools → Agno tools list. EOP steps → agent instructions. Output schema from Acceptance Criteria (A.C.) → Pydantic response_model. Do not add any component not specified in the System Design. If the build reveals a missing element, stop and return to STEP.2 — do not invent design decisions mid-build. | AI assistance in Cursor — Claude co-writes the implementation against the System Design. This makes the translation from System Design to code fast and accurate. The build step should take a fraction of the time that STEP.2 took. If it takes longer, the System Design was underspecified. | AI assistance → implementation follows the System Design exactly → the build step is a fast translation, not a design exploration → no new design decisions are made mid-build → any gap between System Design and code is visible immediately and triggers a clean return to STEP.2. | P1 (ELF → System Design): the System Design from STEP.2 is the blueprint. P2 (hypothesis-driven build): the Acceptance Criteria (A.C.) from STEP.3 constrain exactly what gets built. STEP.4 is purely implementation — all design thinking was done in STEP.2. | Cursor IDE (WORKSPACE.1), INTEL.1 Agno + Claude, INFRA.1 Python runtime, INFRA.2 API access, WORKSPACE.2 Git for the post-build commit. | Cursor open with the ILE Learning Book visible as a reference tab. System Design displayed alongside the code file being written. stream_events=True set before the first test run. No internet browsing for "better approaches" mid-build — the approach was decided in STEP.2. | User starts adding features not in the System Design because "it would be better with X" → scope creep → the resulting agent no longer matches the Acceptance Criteria (A.C.) → STEP.5 test fails on the unexpected additions → P3 causal trace is confused between the designed elements and the additions. | Scope creep mid-build — the excitement of building generates new ideas that were not in the System Design. Each addition is a new design decision made without going through P1 — it bypasses the ELF and inserts an unvalidated assumption directly into the build. | Scope creep → agent grows beyond the System Design → Acceptance Criteria (A.C.) were written for the System Design, not for the expanded agent → some criteria fail on the additions, not on the design → P3 causal trace cannot cleanly identify the source of failure → loop degrades in precision. | "I'll add this one extra thing — it's small." No addition is too small to require a System Design decision. If it belongs in the agent, it should appear in the UES layer of the System Design. If it's not in the System Design, it doesn't belong in the build. | Cursor chat that generates "improved" agent code beyond the System Design spec. Any mid-build Google search for "better agent patterns" that introduces unconsidered approaches. | Building in a state of creative excitement without the System Design document visible. No discipline rule: "if it's not in the System Design, it doesn't get built." | If scope creep has already occurred: strip the additions back to the System Design spec. Test the stripped version against the Acceptance Criteria (A.C.). If the additions were genuinely valuable, go back to STEP.2, add them to the System Design, and then rebuild. | STEP.4 gate: agent code implements the System Design (no more, no less), committed to Git, stream_events=True confirmed active. Then STEP.5 begins. |
| **STEP.5 · Test against Acceptance Criteria (A.C.) — advance or return** *(Activates: P2 decision gate, INTEL.2, INFRA.2)* | This is the decision gate. The agent runs against real data. The Acceptance Criteria (A.C.) from STEP.3 are checked — pass or fail, no middle ground. The decision (advance or return) is made here, before any further work is done. This step closes the P2 hypothesis-driven loop and generates the evidence base for the P3 trace in STEP.6. | The binary decision gate: the agent runs against real domain data, the AgentOS event stream is read end-to-end, each A.C. criterion is checked, and an advance (all pass) or return-to-STEP.2 (any fail) decision is recorded. IS a binary decision point with no middle outcome: 'close enough' is not a pass. IS NOT an informal quality impression: the event stream must be read even when the output looks correct, because a correct output produced by flawed agent reasoning is an unvalidated design assumption that will fail under different inputs. IS NOT done until the specific pass/fail evidence from the event stream is recorded in the ILE Learning Book. | Run the agent. Read the event stream. Check criteria one by one. Record each result (pass/fail + evidence from event stream). Make the advance/return decision based only on the criteria — not on impression of overall quality. **Gate to advance:** all Acceptance Criteria (A.C.) pass AND event stream read end-to-end AND results recorded. Gate to return: any criterion fails → specific failure event identified in stream → return to STEP.2 with that specific element as the revision target. | AgentOS event-stream discipline — the discipline to read the full event stream before making the advance/return decision, even when the output looks correct. A passing output with a suspicious event stream is not a clean pass — it is a design assumption that happened to succeed this time. | Event-stream discipline → the decision is based on both output AND process → design assumptions are validated at the decision level, not just the output level → clean passes mean the System Design is confirmed → failures are diagnosed at the precise event where the assumption broke → STEP.2 revision is surgical, not speculative. | P2 (hypothesis-driven build): the pass/advance decision is the core of P2. It is made here against the pre-committed criteria — not post-hoc against impression. P3 (feedback loop): the event stream read in this step is the raw material for the causal trace in STEP.6. | INTEL.2 AgentOS (event stream), INFRA.2 Anthropic API (real data test call), WORKSPACE.3 ILE Learning Book (to record results and failure event if applicable). | A quiet slot immediately after the STEP.4 build — test and read event stream in the same session, not the next day. Kill criteria checklist open in ILE Learning Book. No celebrating "it ran" before checking all criteria. | User sees that the output "looks right" and declares a pass without checking each kill criterion or reading the event stream → undiscovered criterion failure or suspicious event → advances to STEP.6 with an invalid pass → the P3 causal trace attributes success to design decisions that were not actually validated → the System Design accumulates unconfirmed assumptions. | Output-level pass — declaring success based on the final output without reading the event stream or checking each kill criterion systematically. The output may look right because the model compensated for a design flaw rather than because the design was sound. | Output-level pass → unvalidated design assumptions persist → future builds that rely on those assumptions fail in ways that cannot be traced → P3 loop precision degrades → the system becomes harder to debug with each cycle rather than easier. | "The output looks great — close enough." Kill criteria are binary. "Close enough" is not a pass. Either all criteria are met or the decision is to return to STEP.2. There is no partial advance. | Looking only at the final output without opening the event stream. Making the advance/return decision before all criteria are checked. | No Acceptance Criteria (A.C.) checklist open during testing. Event stream suppressed or not set. Testing done days after the build (context lost). | If you realise a pass was declared without full criteria check: do not continue to STEP.6. Re-run the agent now, check all criteria, read the event stream. Make the correct decision. One correct cycle is worth more than two partial ones. | STEP.5 gate to advance: all Acceptance Criteria (A.C.) pass, event stream read, results recorded in ILE Learning Book. Gate to return: failure event identified and recorded, STEP.2 revision target specified. |
| **STEP.6 · Close the feedback loop — measure, trace, update System Design** *(Activates: P3, WORKSPACE.3, INTEL.2)* | This step completes the P1→P2→P3 cycle and sets up the next one. The measurement from STEP.5 is traced back to the specific System Design decision that produced it. The System Design is updated. Only after this step is the current agent complete and the next agent ready to begin at STEP.2. Without STEP.6, experience accumulates but skill does not compound. | The loop-closing step executed before the next STEP.2 begins: writing the causal trace (one sentence per criterion: '[criterion X] passed/failed ← [System Design decision Y] because [reason Z]'), extracting one transferable decomposition principle, updating the System Design, and committing to Git. IS the ELF Measure step — structurally required for the loop to compound. IS NOT a summary of what happened: it is a causal sentence linking outcome to a specific design decision. IS NOT deferrable to the next session: written in the same session as the build while event stream context is accessible. IS NOT complete if the principle cannot be applied in the next build without knowing this specific context. | Write causal traces immediately after STEP.5 — not in a later session. Use the event stream from STEP.5 as the evidence base: each trace must point to a specific event in the stream, not to a general impression. Write one principle. Update the System Design. Commit. **Gate:** ILE Learning Book updated and committed to Git before the next STEP.2 begins. | Causal traceability discipline + completion momentum redirected — the satisfaction of passing Acceptance Criteria (A.C.) creates energy. STEP.6 redirects that energy toward the 15-minute extraction act before it dissipates into starting the next build. The gate is what captures the energy and converts it into compound learning. | Energy redirected → causal trace is written while the build is still fresh → event stream details are recalled with accuracy → principle is articulated precisely → System Design update is targeted, not vague → next STEP.2 starts with a better design than the current one → each cycle's output quality increases. | P3 (feedback loop): STEP.6 IS the application of P3. The causal trace closes the loop. The principle extraction converts experience into knowledge. The System Design update makes the next loop faster. Without STEP.6, the P1→P2→P3 loop is structurally incomplete and cannot compound. | WORKSPACE.3 ILE Learning Book (causal trace + principle + System Design update), INTEL.2 AgentOS event stream (evidence base for the trace), WORKSPACE.2 Git (commit the updated Learning Book). | Immediately after STEP.5 — same session, same energy. ILE Learning Book open with STEP.5 results visible. Event stream output still accessible. A 15-minute time block allocated before the session closes or the next task begins. | User passes Acceptance Criteria (A.C.), closes Cursor, starts the next task. STEP.6 is skipped. The principle that made this agent work is never extracted. The next agent's STEP.2 starts from the same System Design rather than an improved one. Two cycles later, the user cannot explain why the first agent worked. | Completion momentum — the satisfaction of a successful test creates immediate pressure to start the next thing. STEP.6 feels like a slowdown when the real work ("building the next agent") could begin. In reality, STEP.6 IS work — it is the compounding work. | Skipping STEP.6 → principles stay tacit → each new agent's STEP.2 starts from the same base → decomposition skill does not compound → multi-domain OE system remains perpetually one step away → the vision is never reached, not because building is too hard, but because learning is not being captured. | "I'll write up the learning later." Later never comes at the required level of detail. The event stream is closed, the context is gone, the build energy has dissipated. A causal trace written immediately is worth 10x one written from memory a week later. | Starting STEP.2 of the next agent before STEP.6 of the current agent is complete. Using "I remember what worked" as a substitute for a written causal trace. | No 15-minute reflection block built into the session plan after STEP.5. ILE Learning Book not open at the time of the STEP.5 result. | If STEP.6 was skipped: reconstruct retrospectively now, before starting STEP.2 of the next agent. Use the Git history and any available event logs. Even a partial trace is better than none. Then establish the personal rule: STEP.6 is non-negotiable before STEP.2 restarts. | STEP.6 gate: causal trace written (one sentence per criterion), transferable principle extracted (one sentence), System Design updated, committed to Git. Then STEP.2 of the next agent begins. |
| **STEP.7 · Expand scope incrementally — single agent → team → workflow** *(Activates: P1 + P2 + P3 loop × N)* | Each completed STEP.6 closes one agent's cycle. STEP.7 is not a new type of step — it is the STEP.2→STEP.6 loop repeated for successive agents, with each iteration expanding scope: from one agent to a 2-agent team, from a team to a multi-step workflow, from a workflow to a deployed platform. Scope expands only at gate boundaries, never mid-cycle. | The gated repetition of STEP.2→STEP.6 across expanding scope levels — single agent → 2-agent team → multi-step workflow → deployed platform — with each scope expansion beginning only after the prior level's STEP.6 gate is passed. IS not a new type of step: it is the core loop applied repeatedly at each scope level, each time starting from the accumulated principles of prior STEP.6s. IS NOT all-at-once architecture design: the next scope level's STEP.2 cannot begin until the current scope level's STEP.6 is complete. IS NOT linear in difficulty: the 3rd STEP.2 should take half the time of the 1st because prior principles reduce decomposition uncertainty. | Each expansion phase begins with a new STEP.2 (ELF applied to the expanded scope). The Acceptance Criteria (A.C.) at STEP.3 scale with the scope: single agent criteria → team criteria → workflow criteria. The loop runs at every level. **Gate to expand scope:** current scope's STEP.6 complete before the next scope level begins. Never design the workflow before the 2-agent team is working. | Incremental scope discipline — the personal rule that scope expands only after the current scope's Acceptance Criteria (A.C.) are met and causal trace is written. The discipline prevents the system from becoming large before it is reliable. | Incremental discipline → each expansion starts from a confirmed, causally understood base → when failures occur at the expanded scope, they can only originate from the new addition, not from the base → P3 traces are clean at every level → the OE system compounds in reliability as it grows in scope. | P1 (ELF at each scope level): every expansion is a new System Design subject — the 2-agent team needs its own ELF run. P2 (Acceptance Criteria (A.C.) at each level): each scope level has its own Acceptance Criteria (A.C.). P3 (loop at each level): each expansion generates a causal trace that compounds the decomposition skill. | Full component stack (all 7 from T0.P4) active at every iteration. WORKSPACE.3 ILE Learning Book accumulates one System Design and one principle per agent per scope level. WORKSPACE.2 Git history shows the expansion as a readable build sequence. | A strict scope discipline rule: "I will not design the next scope level until the current one passes Acceptance Criteria (A.C.)." No exceptions. The compounding benefits of this rule become visible by the third agent — the third STEP.2 takes half the time of the first. | User completes the first agent, immediately designs a 7-agent architecture, and tries to build all agents simultaneously → scope jumps from one agent to a full team without intermediate Acceptance Criteria (A.C.) → the team fails in ways that cannot be isolated to a specific agent → P3 traces are impossible → scope collapses back to zero. | Over-ambition at scope expansion — the same blocker as the root UBS (Capability Gap) manifests again at STEP.7. The first working agent creates a sense of mastery that tempts the user to skip scope levels. | Over-ambition → multi-agent architecture designed before a 2-agent team is validated → the architecture assumes design patterns not yet learned → the team fails in combinatorially complex ways → no clean P3 trace possible → confidence collapses → the OE system is abandoned before the compounding begins. | "I understand agents now — I can design the full team." One working agent demonstrates that you can build one agent. It does not demonstrate that you can design a 7-agent coordination system. Each scope level is a new design problem requiring its own ELF. | Architecture design tools (diagrams, whiteboards) used to plan a full team before a 2-agent team is working. Any tool that enables designing beyond the current confirmed scope. | No scope discipline rule. No explicit gate between scope levels. Excitement after the first working agent with no structured next step to channel it. | If scope has jumped ahead: do not abandon the advanced design — park it in the ILE Learning Book as a future System Design candidate. Return to STEP.2 for the 2-agent team. Build the team. Pass the gate. Then revisit the advanced design with real team-level experience to validate or revise it. | STEP.7 gate per scope level: each agent's STEP.6 complete before the next agent's STEP.2 begins. Scope expands from single agent → 2-agent team → multi-step workflow → deployed platform — in sequence, gated, one level at a time. |
| **STEP.8 · Extract, package and document the OE meta-system — so other solo operators can use it** *(Activates: P3 at meta-level, WORKSPACE.3)* | This is where the domain-specific work becomes the meta-system — and where the meta-system becomes usable by others. After the first complete agent team is working, the user extracts the reusable patterns, packages them as named OE components, and writes clear documentation so that any other solo operator can pick up the OE system and build their own domain-specific UE system without needing to rediscover what you already learned. The deliverable is not just a personal template library — it is a portable, documented OE system. | The meta-extraction step: separating domain-agnostic patterns from domain-specific implementation across the first complete team, naming each reusable pattern as a documented OE component, and writing an OE README that enables a second solo operator to run STEP.1–7 using only the README and component templates. IS the step that converts domain-specific work into a portable meta-system — the actual UDO deliverable. IS NOT documentation of what was built: it is specification of what is reusable and how to reuse it. IS NOT complete until a second domain test confirms the README reduces STEP.2 time: untested documentation is not a functional OE component. | Allocate a dedicated packaging session — no new building. Go through each agent, team, and workflow component. For each, write: "domain-specific:" and "domain-agnostic:". Extract domain-agnostic portion as a named template. Write the OE README. Commit all documentation to Git under a `/oe-system/` directory. Test the documentation by running STEP.2 for a second domain using only the README and templates. **Gate:** OE README committed, at least one named template committed, second domain STEP.2 time measurably shorter than first domain. | Deliberate extraction + documentation discipline — extracting without documenting only produces private knowledge. Writing the OE README forces the operator to articulate the system at a level that an outsider can follow. The act of writing for another operator reveals every implicit assumption that was never captured. If you cannot write the README clearly, the meta-system is not yet understood well enough. | Documentation for others → every implicit assumption must be made explicit → the OE system is stress-tested at the conceptual level by being articulated for a stranger → weak patterns are exposed during writing, not during a second operator's first confused build → the OE meta-system is genuinely generalisable, not just reusable by its creator. | P3 (feedback loop at meta-level): STEP.8 is P3 applied to the entire first build cycle. Causal trace: "The domain-agnostic pattern in this team is [X] because [reason Y]." System Design update: "This pattern is available as a named OE component in the README." P1: writing the README requires re-applying the ELF to the OE system itself — treating the OE system as the agent being designed for the next operator. | WORKSPACE.3 ILE Learning Book (OE component library + README section), WORKSPACE.2 Git (`/oe-system/` directory for templates and README), INTEL.1 Claude (to review the README for clarity: "Would a new solo operator understand this?"). | A dedicated packaging session immediately after STEP.7 — same energy, no new build. A real or hypothetical second domain problem to test the documentation. A personal rule: the OE system is not complete until a second operator could run STEP.1–7 from the README alone. | User finishes the first team, immediately starts building the second domain from scratch, treats the first build as a portfolio piece rather than a documented template source → second domain takes as long as the first → no other operator can benefit → OE system remains private and brittle → the "system that builds systems for anyone" vision is never realised. | First-instance specificity + documentation aversion — the first working team is always more domain-specific than intended, and writing documentation feels slower than building. Both forces push against STEP.8. Without a gate, neither is overcome. | First-instance specificity → patterns stay domain-specific and implicit → second operator starts from scratch → OE system is not portable → the meta-system vision collapses to "my personal template folder" → solo operator ceiling is never broken for others. | "Other operators can figure it out from the code." Code is the least accessible documentation for a non-technical solo operator. The README is the access point. Without it, the OE system is closed to everyone except its creator. | Starting the second domain build before packaging is complete. Treating code files as documentation. Skipping the second domain test on the grounds that "it would obviously work." | No `/oe-system/` directory in Git. No README template. No rule that STEP.8 must be complete before the second domain's STEP.2 begins. | If STEP.8 has been skipped: pause all new domain building and run the packaging session now. Review the existing team retrospectively. The patterns exist — they need naming, documenting, and a README. Retrofitting documentation is lower cost than rebuilding the second domain from scratch, and far lower cost than watching the OE vision fail for lack of a usable system. | STEP.8 gate: at least one named, domain-agnostic OE component documented in ILE Learning Book, OE README written (≤2 pages, runnable by a new solo operator), both committed to Git under `/oe-system/`, and second domain STEP.2 completed faster using the README. The OE meta-system is now active and portable. |

---

*1 table · 8 rows · 16 columns · Topic 0, Page 5*
*Steps 1–6 = one complete P1→P2→P3 cycle for one agent. Step 7 = cycle repeated across scope levels. Step 8 = cycle applied at meta-level to produce the OE system.*
*Each step references: P1 (T0.P3), P2 (T0.P3), P3 (T0.P3), or components from T0.P4.*
*Topic 0 complete after this page. Topic 1 begins with a copy of T0.P1 as T1.P0.*
